# Правила сдачи домашних заданий на курсе

Все домашние задания на курсе сдаются через github. Вы делаете приватный репозиторий, в котором настраиваете upstream и сдаете задания через pull request. Далее приведена подробная инструкция. Если с каким-либо из шагов у вас возникнут проблемы, не стесняйтесь задавать их в чате курса. 

**Пошаговая инструкция (как настроить репозиторий):**

1. Создайте пустой репозиторий. Название может быть любым (но лучше, чтобы оно было связано с названием курса). **ВАЖНО!:** репозиторий обязательно должен быть приватным.
2. **Важно** именно пустой репозиторий (то есть без каких либо файлов и коммитов) – иначе у вас возникнут проблемы со следующими пунктами
3. Вам необходимо добавить преподавательский состав (преподавателей и ассистентов) в свой репозиторий. (`settings -> collaborators -> add people`). Ники находятся на главной странице [README.md](./README.md). Для удобства, мы продублируем их в канале. 
4. Клонируете репозиторий на свой компьютер. (`git clone <АДРЕС РЕПОЗИТОРИЯ>`) (`АДРЕС РЕПОЗИТОРИЯ` – ваш репозиторий)
5. Настраиваете upstream с этим репозиторием – `git remote add upstream <АДРЕС ОСНОВНОГО РЕПОЗИТОРИЯ КУРСА>` (`АДРЕС ОСНОВНОГО РЕПОЗИТОРИЯ КУРСА` – общий репозиторий)
6. Делаете pull upstream репозитория – `git pull upstream main`
7. На данном этапе у вас должна получиться точная копия данного репозитория. Если это не так, можете проконсультироваться в чате курса, мы вам поможем.
8. Далее вы будете сдавать домашние задания, через новую ветку и создание pull request. Все шаги описаны в следующей инструкции

**Пошаговая инструкция (как сдавать домашние задания):**

1. Прежде всего переходим в `main` ветку – `git checkout main` 
2. Перед тем, как приступить к выполению, убедитесь, что вы обновили свой репозиторий с помощью команды `git pull upstream main`.
3. Не забываем синковать свой локальный `main` с удаленным: `git push origin main` (чтобы при pull requests домашних заданий проверяющие будут видеть только нужные файлы)
4. Создаем новую ветку для выполнения домашнего задания. Название ветки задаем в формате `lesson_<НОМЕР>_hw`. Например, для сдачи 1го задания нужно будет создать ветку `lesson_1_hw` – `git checkout -b lesson_1_hw`
5. Усердно трудимся, чтобы выполнить все задания
6. Добавляем и пушим файлы `git add <ФАЙЛЫ> && git commit -m '<КОММЕНТАРИЙ>' && git push origin lesson_<НОМЕР>_hw` (в первый пуш может потребоваться `git push --set-upstream origin lesson_<НОМЕР>_hw`)
7. Создаем pull request. Для этого переходим на страницу репозитория, выбираем созданную ветку и нажимаем кнопку `Compare & pull request`. В заголовке pull request указываем название задания.
8. Ждем, пока преподаватели проверит и прокомментируют ваше решение.
9. **ВАЖНО!** не делаем коммиты в ветку после дедлайна, до комментариев от преподавателей. Проверяющий ваше задание будет ориентироваться на дату сдачи задания по дате последнего коммита. Если вы будете коммитить после дедлайна, то можете случайно получить снижение баллов
10. Если проверяющий оставил вам комментарии с просьбой доработки – вы можете это поправить, и это не будет считаться сдачей после дедлайна. Пример: `Привет! Классно справился с задачей, однако код не отфарматирован и плохо читаем. Пока что ставлю 9, если поправишь, то поставлю 10.`
11. Если вы делаете доработки по комментариям проверяющего, пожалуйста, тегните его, чтобы проверить ваши изменения (делается в комментарии к pull request). 
12. **ВАЖНО!** pull request **не надо** сливать в `main`, чтобы избежать проблем в последующем (если смержите, то `git pull upstream main` может не пройти).
